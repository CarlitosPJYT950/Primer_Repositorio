package settings;
public class Settings {
    public static void main(String[] args) {
        // TODO code application logic here
        Logica<String> caja = new Logica<>();
        caja.ponerAlgo("Juan");
        String contenido = caja.obtenerAlgo();
        
        System.out.println("El contenido es: " + contenido);
        
        Logica<Integer> enteros = new Logica<>();
        enteros.ponerAlgo(25);
        Integer numero = enteros.obtenerAlgo();
        
        System.out.println("El contenido es: " + numero);
    }
}