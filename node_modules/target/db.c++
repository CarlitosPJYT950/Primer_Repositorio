#include <iostream>
using namespace std;

struct person {
    string name;
    int age;
};

void puntero (person *p, int edad) {p->age = edad;}
void np (person p, int edad) {p.age = edad;}

int main(){
    person persona = {"Juan", 24};
    cout << "Inicio: " << persona.name << " tiene " << persona.age << " años." << endl;

    np (persona, 25);
    cout << "Después de np: " << persona.name << " tiene " << persona.age << " años." << endl;

    puntero (&persona, 26);
    cout << "Después de puntero: " << persona.name << " tiene " << persona.age << " años." << endl;

    return 0;
}