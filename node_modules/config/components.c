#include <stdio.h>
#include <string.h>

struct person{
	char name[5];
	int age;
	float high;
};

int main(void){
	struct person p1;

	strcpy(p1.name, "Juan");
	p1.age = 24;
	p1.high = 1.6;

	printf("Name: %s\n", p1.name);
	printf("Age: %d\n", p1.age);
	printf("High: %f\n", p1.high);

	return 0;
}